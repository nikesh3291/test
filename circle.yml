general:
  branches:
    only:
      - develop # branches to build
      - /QA_.*/


notify:
  webhooks:
    # A list of hook hashes, containing the url field
    - url: https://circleci.com/gh/nikesh3291/test
